# --- Appearance ---
theme = "Catppuccin Mocha"

# Make blur effective (requires opacity < 1.0)
background-opacity = 0.95 
background-blur-radius = 20

# Background Image
# background-image = "/Users/lux/Library/Mobile Documents/com~apple~CloudDocs/Wallpapers/cyberpunk.jpg"
# background-image-opacity = 0.1
# background-image-fit = cover

# --- Font & Text ---
font-family = "BerkeleyMono Nerd Font"
font-family = "LXGW WenKai"
font-size = 14
font-feature = liga,calt,dlig
font-thicken = true
# Adjusted to pixels (no percentage allowed)
adjust-cell-height = 6%

# --- Window ---
window-height = 35
window-width = 120
window-padding-x = 12
window-padding-y = 8
macos-titlebar-style = tabs
macos-option-as-alt = true

# --- Cursor & Mouse ---
cursor-style = block
# Minimum thickness: 1 pixel
adjust-cursor-thickness = 2
cursor-style-blink = true
cursor-opacity = 0.85
custom-shader = ~/.config/ghostty/shaders/shader.glsl

# Hide mouse when typing
mouse-hide-while-typing = true

# Scroll speed multiplier
mouse-scroll-multiplier = 1.0

# --- Performance & System ---
shell-integration = zsh
shell-integration-features = no-cursor
copy-on-select = clipboard
quit-after-last-window-closed = true
link-url = true

# Updates (Using 'tip' channel for latest features/nightly builds)
auto-update = check
auto-update-channel = tip

# --- Keybinds ---
keybind = super+i=inspector:toggle
keybind = super+r=reload_config
